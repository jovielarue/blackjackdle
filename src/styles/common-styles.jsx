import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  screenView: {
    width: '100%',
    height: '100%',
    backgroundColor: '#2B6030',
    display: 'flex',
    alignItems: 'center',
  },
  headerText: {
    fontSize: 30,
    color: 'white',
    fontWeight: 600,
    textAlign: 'center',
  },
});
